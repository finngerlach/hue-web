{
  "name": "hue-web",
  "version": "1.0.0",
  "main": "index.js",
  "repository": "git@github.com:finngerlach/hue-web.git",
  "scripts": {
    "start": "DOCKER_BUILDKIT=1 docker-compose -f docker-compose.dev.yml build && docker-compose -f docker-compose.dev.yml up"
  }
}
